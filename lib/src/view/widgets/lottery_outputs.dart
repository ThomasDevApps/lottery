import 'package:flutter/material.dart';

class LotteryOutputs extends StatelessWidget {
  const LotteryOutputs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
